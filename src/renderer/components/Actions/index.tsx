import * as React from "react";
import ToolbarButton from './../ToolbarItem/ToolbarItem';
import { icons } from '@/renderer/constants/icons';

export const Actions = () => (
    <>
        <ToolbarButton icon={icons.settings} size={18} />
    </>
)