import styled from "styled-components";

export const BubbleContainer = styled.div`
    display: flex;
    flex-direction: row;
    margin: 18px 0;

    &:nth-child(1) {
        margin-left: 0px;
    }
`;